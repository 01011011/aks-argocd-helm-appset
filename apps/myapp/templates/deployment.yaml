apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
        - name: myapp
          image: nginx:latest
          ports:
            - containerPort: 80
          env:
            - name: CLIENT_ID
              value: {{ with (lookup "v1" "ConfigMap" "default" "argo-runtime-config") }}{{ .data.client_id | default "" }}{{ else }}{{ "" }}{{ end }}
            - name: VAULT_NAME
              value: {{ with (lookup "v1" "ConfigMap" "default" "argo-runtime-config") }}{{ .data.vault_name | default "" }}{{ else }}{{ "" }}{{ end }}
            - name: REGISTRY_URL
              value: {{ with (lookup "v1" "ConfigMap" "default" "argo-runtime-config") }}{{ .data.registry_url | default "" }}{{ else }}{{ "" }}{{ end }}
